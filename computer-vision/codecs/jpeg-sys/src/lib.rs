
// C Code doesn't follow rust styles.
// generated code doesn't follow styles either
// tell the compiler we don't care about style
// there should be an intermediate library that cleans up the style
#![allow(non_camel_case_types, non_upper_case_globals, non_snake_case)]

// nothing to do, we are a simple library that exports the generated bindings
include!(concat!(env!("OUT_DIR"), "/bindings.rs"));